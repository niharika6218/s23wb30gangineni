extends layout

block content
  h1= title
  p this is niharika and i like to do drawing 
  button(type="button", class="btn btn-warning" onclick="doReduce()") Press Me
  div#reduce Reduce the value
  br
  a(href="university") University
  script.
    let count = 20;
    let change = -1;
    function doReduce() {
      count += change;
      change -= 1;
      document.getElementById('reduce').textContent = `Count ${count} with change ${change}.`;
    }
